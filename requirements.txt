# Pinned because OpenCV 5 moved several of the calls this repo uses (the ArUco
# module, some free functions) and the numbers quoted in the README were
# measured on this exact build.
opencv-contrib-python==4.14.0.94
numpy>=1.24
matplotlib>=3.6
pytest>=7.0
